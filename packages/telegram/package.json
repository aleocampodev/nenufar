{
  "name": "@nenufar/telegram",
  "version": "2.1.0",
  "private": true,
  "description": "Telegram command handler — /nuevo, /pagado, /despachado, /pedido, /pendientes, /help, photo-to-draft. Idempotent.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nenufar/types": "workspace:*",
    "@nenufar/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}