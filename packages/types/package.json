{
  "name": "@nenufar/types",
  "version": "2.1.0",
  "private": true,
  "description": "Shared types — payload-types (generated) + Zod schemas for command validation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}